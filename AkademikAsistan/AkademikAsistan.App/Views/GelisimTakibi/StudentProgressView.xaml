<UserControl x:Class="AkademikAsistan.App.Views.GelisimTakibi.StudentProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:AkademikAsistan.App.ViewModels.GelisimTakibi"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:StudentProgressViewModel}"
             Background="{StaticResource ParchmentBrush}">

    <!-- Code-behind'da sadece InitializeComponent() vardır. -->

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32">
        <StackPanel MaxWidth="1100" HorizontalAlignment="Left">

            <TextBlock Text="Gelişim Takibi" Style="{StaticResource PageTitleText}"/>
            <TextBlock Text="Dönemlik GNO / DNO değişiminizi izleyin."
                       Style="{StaticResource BodyText}" Foreground="{StaticResource SlateBrush}"
                       Margin="0,4,0,24"/>

            <!-- ===== Grafik Kartı ===== -->
            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,28">
                <StackPanel>
                    <TextBlock Text="Not Ortalaması Trendi" Style="{StaticResource SectionTitleText}" Margin="0,0,0,16"/>
                    <lvc:CartesianChart Series="{Binding GpaSeries}"
                                        XAxes="{Binding XAxes}"
                                        YAxes="{Binding YAxes}"
                                        Height="320"/>
                </StackPanel>
            </Border>

            <!-- ===== Dönem Ekleme Formu ===== -->
            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,28">
                <StackPanel>
                    <TextBlock Text="Yeni Dönem Kaydı Ekle" Style="{StaticResource SectionTitleText}" Margin="0,0,0,16"/>

                    <UniformGrid Columns="4" Rows="1">
                        <StackPanel Margin="0,0,16,0">
                            <TextBlock Text="DÖNEM ADI" Style="{StaticResource LabelText}"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding NewSemesterName, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,0">
                            <TextBlock Text="DÖNEMLİK ORT. (DNO)" Style="{StaticResource LabelText}"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding NewSemesterGpa, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,0">
                            <TextBlock Text="GENEL ORT. (GNO)" Style="{StaticResource LabelText}"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding NewCumulativeGpa, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,0">
                            <TextBlock Text="TOPLAM KREDİ" Style="{StaticResource LabelText}"/>
                            <TextBox Style="{StaticResource FormTextBox}" Text="{Binding NewTotalCredits, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </UniformGrid>

                    <Button Content="Kaydı Ekle" Style="{StaticResource PrimaryButton}"
                            Command="{Binding AddSemesterCommand}"
                            HorizontalAlignment="Right" Margin="0,16,0,0"/>

                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}"
                               Margin="0,12,0,0" TextWrapping="Wrap"
                               Visibility="{Binding ErrorMessage, Converter={StaticResource NullOrEmptyToVisibilityConverter}}"/>
                </StackPanel>
            </Border>

            <!-- ===== Dönem Listesi ===== -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="Dönem Kayıtları" Style="{StaticResource SectionTitleText}" Margin="0,0,0,12"/>
                    <DataGrid Style="{StaticResource MinimalDataGrid}" ItemsSource="{Binding Records}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Dönem" Binding="{Binding SemesterName}" Width="*"/>
                            <DataGridTextColumn Header="DNO" Binding="{Binding SemesterGpa}" Width="100"/>
                            <DataGridTextColumn Header="GNO" Binding="{Binding CumulativeGpa}" Width="100"/>
                            <DataGridTextColumn Header="Toplam Kredi" Binding="{Binding TotalCredits}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
